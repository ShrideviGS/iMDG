<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core" class="sapUiSizeCompact" xmlns:l="sap.ui.layout" xmlns:table="sap.ui.table">
	<table:Table rows="">
		<table:columns>
			<table:Column>
				<Label text="{i18n>CLASS_TYPE}"/>
				<table:template>
					<Text text="" wrapping="false"/>
				</table:template>
			</table:Column>
			<table:Column >
				<Label text="{i18n>CLASS_NAME}"/>
				<table:template>
					<Text text="" wrapping="false"/>
				</table:template>
			</table:Column>
			<table:Column>
				<Label text="{i18n>FIELD_NAME}"/>
				<table:template>
					<Text text="" wrapping="false"/>
				</table:template>
			</table:Column>
			<table:Column>
				<Label text="{i18n>DISPLAY_NAME}"/>
				<table:template>
					<Text text="" wrapping="false"/>
				</table:template>
			</table:Column>
			<table:Column>
				<Label text="{i18n>FIELD_VALUE}"/>
				<table:template>
					<Text text="" wrapping="false"/>
				</table:template>
			</table:Column>
			<table:Column>
				<Label text="{i18n>UNIT}"/>
				<table:template>
					<Text text="" wrapping="false"/>
				</table:template>
			</table:Column>
			<table:Column>
				<Label text="{i18n>FIELD_DESCRIPTION}"/>
				<table:template>
					<Text text="" wrapping="false"/>
				</table:template>
			</table:Column>
			<table:Column>
				<Label text="{i18n>ADD}"/>
				<table:template>
					<Text text="" wrapping="false"/>
				</table:template>
			</table:Column>
			<table:Column>
				<Label text="{i18n>DELETE}"/>
				<table:template>
					<Text text="" wrapping="false"/>
				</table:template>
			</table:Column>
		</table:columns>
	</table:Table>
</core:FragmentDefinition>